//! Entrance Analyser backend — scaffold only.
//!
//! The real server (axum, routes, storage) lands in PR 2. This entry point
//! exists so that `cargo check` and `cargo test` pass in CI from PR 1.
fn main() {
    println!("entrance-analyser-backend: scaffold (see PR 2 for the real server)");
}
